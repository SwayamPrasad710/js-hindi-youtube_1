/*
 equality check == and comparision > < >= <= works differently. Comparision convert null to a pointer and treating it as 0. Hence, ( null >= 0 ) o/p : true but ( null > 0 ) is false.

*/

// (== normal equality check) / (=== strict check.)